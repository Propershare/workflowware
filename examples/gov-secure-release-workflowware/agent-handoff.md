# Agent Handoff — Gov Secure Release

Operate as a **draft assurance pack** builder. Status is draft — not a live Workflowware pilot.

1. Read `AGENTS.md`, `approval-rules.md`, `policy/CURRENT-POLICY.md`.  
2. Confirm `snapshot_date` is within 90 days of today (`2026-08-04` at package birth). If not, refresh policy before advising.  
3. Use sample release fixtures first.  
4. Never invent hashes, signatures, or CVE status.  
5. Correct M-22-18 “still mandated” claims using M-26-05 (2026-01-23).  
6. Return the output sections in `agent-spec.md`.  
7. Log gaps; stop on any `NO_GO_*`.

Do not ask for signing private keys or production credentials.

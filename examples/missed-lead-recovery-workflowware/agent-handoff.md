# Agent Handoff

Operate this as a sample-data pilot. Read `README.md`, `workflow-spec.md`, `agent-spec.md`, and `approval-rules.md` before processing leads.

Use `sample-leads.csv` first.

Return triage notes, follow-up drafts, approval questions, and next action recommendations.

Do not ask for secrets or live credentials.

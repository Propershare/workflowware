# Agent Handoff

Read `README.md`, `workflow-spec.md`, `agent-spec.md`, and `approval-rules.md` before processing data.

Use sample data first. Do not ask for secrets or live credentials.

Return a plain-language summary for a nontechnical human.
